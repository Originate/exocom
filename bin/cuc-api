#!/usr/bin/env bash

if [ "$#" == "0" ]; then
  EXOCOM_TEST_DEPTH=API cucumber-js --tags ~@todo
else
  EXOCOM_TEST_DEPTH=API DEBUG='*,-express:*,-body-parser:*,-finalhandler,-text-stream-search,-rails-delegate' cucumber-js --tags ~@todo "$@"
fi
