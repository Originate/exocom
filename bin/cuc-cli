#!/usr/bin/env bash

if [ "$#" != "1" ]; then
  EXOCOMM_TEST_DEPTH=CLI cucumber-js --tags ~@todo "$@"
else
  EXOCOMM_TEST_DEPTH=CLI DEBUG='*,-express:*,-body-parser:*,-finalhandler,-text-stream-search' cucumber-js --tags ~@todo "$@"
fi
