#!/usr/bin/env bash

(cd go && ../morula all --never=vendor bin/setup)
(cd haskell && ../morula all bin/setup)
(cd java && ../morula all bin/setup)
(cd javascript && ../morula all bin/setup)
(cd scala && ../morula all bin/setup)
