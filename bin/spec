#!/usr/bin/env bash

killall node 2> /dev/null
set -e
if [ "$#" == "0" ]; then
  bin/lint
  bin/tests
  cucumber-js
else
  DEBUG='*,-express:*,-body-parser:*,-finalhandler,-rails-delegate' cucumber-js "$@"
fi
