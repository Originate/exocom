#!/usr/bin/env bash


killall node 2> /dev/null
set -e
if [ "$#" == "0" ]; then
  mocha --compilers ls:livescript "**/*spec.ls"
  bin/cuc-api
  bin/cuc-cli
else
  bin/cuc-api "$@"
  bin/cuc-cli "$@"
fi
