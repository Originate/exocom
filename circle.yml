machine:

  node:
    version: 5.5.0


test:
  pre:
    - bin/build


deployment:
  publish:
    branch: release
    commands:
      - npm set //registry.npmjs.org/:_authToken $AUTH_TOKEN
      - npm publish
