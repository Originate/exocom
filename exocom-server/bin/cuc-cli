#!/usr/bin/env bash

node_modules/o-tools-livescript/bin/build
if [ "$#" == "0" ]; then
  EXOCOM_TEST_DEPTH=CLI node_modules/.bin/cucumber-js --tags ~@todo
else
  EXOCOM_TEST_DEPTH=CLI DEBUG='*,-express:*,-body-parser:*,-finalhandler,-text-stream-search' node_modules/.bin/cucumber-js --tags ~@todo "$@"
fi
